from app import db

class {{ Model.Name }}(db.Model):
    ID = db.Column(db.Integer, primary_key=True)
    {% for Field in Model.Fields %}
    {{ Field.Name }} = db.Column(db.{{Field.Type}})
    {% endfor %}

    def __repr__(self):
        return('<{{ Model.Name }} {}>'.format(self.ID))
